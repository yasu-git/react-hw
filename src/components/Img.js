import React from 'react'

const Img = (props) => {
    return (

        <div id="main_book_left">
            <img alt="book-img"
                src={props.imageSrc} />
        </div>

    )
}

export default Img
